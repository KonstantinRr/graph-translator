#!/usr/bin/env python3

from src import graph
from src import models

import networkx as nx

if __name__ == '__main__':
    #graph = nx.path_graph(4)
    #models.addMinRequirements(graph)
    #newGraph = models.convert(graph, 'thu_th', 'weight', 'thu')
    graph.runProject()