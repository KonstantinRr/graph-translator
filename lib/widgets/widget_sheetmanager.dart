

import 'package:flutter/material.dart';

class WidgetSheetManager extends StatelessWidget {
  const WidgetSheetManager({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}